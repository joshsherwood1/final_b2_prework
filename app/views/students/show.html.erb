<h2>Class and Grade Information about <%= @student.name %></h2>
<br>
<p><% @student.courses.each do |course|  %>
  <section id="course-<%=course.id%>">
  <%= course.name %>
  <%= @student.find_grade_by_course(course.id).round(2) %>%
  </section>
<% end %>
